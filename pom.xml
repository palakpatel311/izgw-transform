<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
        <groupId>gov.cdc.izgw</groupId>
        <artifactId>izgw-bom</artifactId>
        <version>1.0.3-RELEASE</version>
        <relativePath />
	</parent>
	<groupId>gov.cdc.izgateway</groupId>
	<artifactId>xform</artifactId>
    <version>0.13.1</version>
	<name>xform</name>
	<description>IZ Gateway Xform Service</description>
	<properties>
		<java.version>21</java.version>
		<timestamp>${maven.build.timestamp}</timestamp>
		<maven.build.timestamp.format>yyyyMMddHHmm</maven.build.timestamp.format>
		<project.java.package>${project.groupId}</project.java.package>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.build.finalName>${project.artifactId}-${project.version}-${timestamp}</project.build.finalName>
		<image.tag>${project.build.finalName}</image.tag>
		<buildno>${timestamp}</buildno>
		<xform.testing.common.pass>XFORM_TESTING_COMMON_PASS</xform.testing.common.pass>

		<!-- JaCoCo Configuration -->
		<jacoco.version>0.8.13</jacoco.version>
		<jacoco.jar>${user.home}/.m2/repository/org/jacoco/org.jacoco.agent/${jacoco.version}/org.jacoco.agent-${jacoco.version}.jar</jacoco.jar>
		<surefire.argLine>
			-Xmx2048m
			--add-opens=java.base/java.lang.reflect=ALL-UNNAMED
			--add-opens=java.base/java.net=ALL-UNNAMED
		</surefire.argLine>
	</properties>
	<repositories>
        <repository>
            <id>central</id>
            <name>Maven Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>github-bom</id>
            <name>GitHub Packages</name>
            <url>https://maven.pkg.github.com/palakpatel311/izgw-bom</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>github-core</id>
            <name>izgw-core</name>
            <url>https://maven.pkg.github.com/palakpatel311/izgw-core</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>v2tofhir</id>
            <url>https://maven.pkg.github.com/palakpatel311/v2tofhir</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
            <releases>
                <enabled>true</enabled>
            </releases>
        </repository>
	</repositories>
	<dependencies>
		<!-- Lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>

		<!-- HAPI HL7 v2 - needed for Operations & Preconditions -->
		<dependency>
			<groupId>ca.uhn.hapi</groupId>
			<artifactId>hapi-structures-v251</artifactId>
		</dependency>

		<!-- Apache Camel - Spring Boot -->
		<dependency>
			<groupId>org.apache.camel.springboot</groupId>
			<artifactId>camel-spring-boot-starter</artifactId>
			<version>${camel.version}</version>
		</dependency>

		<!-- Internal IZ Gateway dependencies -->
		<dependency>
			<groupId>gov.cdc.izgw</groupId>
			<artifactId>izgw-core</artifactId>
			<version>3.0.0-izgw-core-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>gov.cdc.izgw</groupId>
			<artifactId>v2tofhir</artifactId>
			<version>2.0.0-v2tofhir-SNAPSHOT</version>
			<exclusions>
				<exclusion>
					<groupId>org.xerial</groupId>
					<artifactId>sqlite-jdbc</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<!-- AspectJ -->
		<dependency>
			<groupId>org.aspectj</groupId>
			<artifactId>aspectjrt</artifactId>
		</dependency>
		<dependency>
			<groupId>org.aspectj</groupId>
			<artifactId>aspectjweaver</artifactId>
		</dependency>

		<!-- Test dependencies -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-site-plugin</artifactId>
					<version>3.21.0</version>
				</plugin>
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>${jacoco.version}</version>
					<executions>
						<execution>
							<id>prepare-agent</id>
							<goals>
								<goal>prepare-agent</goal>
							</goals>
						</execution>
						<execution>
							<id>report</id>
							<phase>verify</phase>
							<goals>
								<goal>report</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-build-label</id>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<phase>generate-resources</phase>
						<configuration>
							<outputDirectory>${project.build.outputDirectory}</outputDirectory>
							<overwrite>true</overwrite>
							<resources>
								<resource>
									<directory>${project.basedir}/docker/webapp/static</directory>
									<filtering>true</filtering>
									<includes>
										<include>build.txt</include>
									</includes>
								</resource>
							</resources>
							<delimiters>
								<delimiter>%{*}</delimiter>
							</delimiters>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<version>3.4.0</version>
				<executions>
					<execution>
						<id>validate</id>
						<phase>validate</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<configLocation>ai-checkstyle.xml</configLocation>
							<consoleOutput>true</consoleOutput>
							<failsOnError>true</failsOnError>
                            <!-- Optionally, specify the location of the violations file -->
							<outputFile>${project.build.directory}/checkstyle-result.xml</outputFile>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<compilerId>javac</compilerId>
					<source>21</source>
					<target>21</target>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<version>${spring-boot.version}</version>
				<configuration>
					<mainClass>gov.cdc.izgateway.xform.Application</mainClass>
					<jvmArguments>-Dspring.application.admin.enabled=true</jvmArguments>
					<requiresUnpack>
						<dependency>
							<groupId>org.bouncycastle</groupId>
							<artifactId>bcpkix-fips</artifactId>
						</dependency>
						<dependency>
							<groupId>org.bouncycastle</groupId>
							<artifactId>bc-fips</artifactId>
						</dependency>
						<dependency>
							<groupId>org.bouncycastle</groupId>
							<artifactId>bctls-fips</artifactId>
						</dependency>
					</requiresUnpack>
				</configuration>
				<executions>
					<execution>
                        <id>default-jar</id>
						<goals>
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.spotify</groupId>
				<artifactId>dockerfile-maven-plugin</artifactId>
				<version>1.4.7</version>
				<dependencies>
					<dependency>
						<groupId>com.github.jnr</groupId>
						<artifactId>jnr-unixsocket</artifactId>
						<version>0.38.14</version>
					</dependency>
				</dependencies>
				<executions>
					<execution>
						<id>build-image</id>
						<phase>install</phase>
						<goals>
							<goal>build</goal>
							<goal>tag</goal>
						</goals>
						<configuration>
							<tag>${image.tag}-${buildno}</tag>
						</configuration>
					</execution>
					<execution>
						<id>tag-latest</id>
						<phase>install</phase>
						<goals>
							<goal>tag</goal>
						</goals>
						<configuration>
							<tag>latest</tag>
						</configuration>
					</execution>
				</executions>
				<configuration>
					<repository>izgw-transform</repository>
					<buildArgs>
						<JAR_FILENAME>${project.build.finalName}.jar</JAR_FILENAME>
						<XFORM_VERSION>${project.version}</XFORM_VERSION>
					</buildArgs>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<environmentVariables>
						<SSL_SHARE>${project.build.directory}</SSL_SHARE>
						<COMMON_PASS>${xform.testing.common.pass}</COMMON_PASS>
                        <XFORM_INIT>true</XFORM_INIT>
                        <XFORM_CRYPTO_CLIENT_CERT_FILE>${project.build.directory}/self_signed_unit_tests.crt</XFORM_CRYPTO_CLIENT_CERT_FILE>
                        <XFORM_CRYPTO_CLIENT_KEY_FILE>${project.build.directory}/self_signed_unit_tests.key</XFORM_CRYPTO_CLIENT_KEY_FILE>
                        <XFORM_CRYPTO_STORE_TRUST_TOMCAT_SERVER_FILE>${project.build.directory}/awsdev_keystore.bcfks</XFORM_CRYPTO_STORE_TRUST_TOMCAT_SERVER_FILE>
                        <XFORM_CRYPTO_STORE_TRUST_WS_CLIENT_FILE>${project.build.directory}/izgw_client_trust.bcfks</XFORM_CRYPTO_STORE_TRUST_WS_CLIENT_FILE>
                        <XFORM_CRYPTO_STORE_KEY_WS_CLIENT_FILE>${project.build.directory}/awsdev_keystore.bcfks</XFORM_CRYPTO_STORE_KEY_WS_CLIENT_FILE>
					</environmentVariables>
				</configuration>
			</plugin>
			<plugin>
                <!-- Since all libraries are deliverable, this should in fact be
                            the only dependency check required.
                         -->
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>12.1.1</version>
				<configuration>
                    <!--nvdApiKey>${env.NVDAPIKEY}</nvdApiKey-->
					<formats>
						<format>html</format>
						<format>json</format>
					</formats>
                    <!-- Disable .NET Analyzer -->
					<assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
					<nugetconfAnalyzerEnabled>false</nugetconfAnalyzerEnabled>
					<nuspecAnalyzerEnabled>false</nuspecAnalyzerEnabled>
                    <!-- Fail builds where this is used to 7 High to Critical -->
					<failBuildOnCVSS>7</failBuildOnCVSS>
                    <!-- Don't fail on error (for cases where CVE REPO is bad) -->
					<failOnError>false</failOnError>
					<skip>${skipDependencyCheck}</skip>
					<suppressionFile>${project.basedir}/dependency-suppression.xml</suppressionFile>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
	<reporting>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.12.0</version>
				<configuration>
					<failOnError>false</failOnError>
					<additionalJOption>-Xdoclint:none</additionalJOption>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-project-info-reports-plugin</artifactId>
				<version>3.6.1</version>
				<reportSets>
					<reportSet>
						<reports>
							<report>index</report>
							<report>dependencies</report>
							<report>dependency-info</report>
							<report>dependency-management</report>
							<report>distribution-management</report>
							<report>licenses</report>
							<report>scm</report>
							<report>summary</report>
						</reports>
					</reportSet>
				</reportSets>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-report-plugin</artifactId>
				<version>3.5.3</version>
			</plugin>
			<plugin>
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>8.4.3</version>
				<configuration>
					<!--nvdApiKey>${env.NVDAPIKEY}</nvdApiKey-->
					<formats>
						<format>HTML</format>
					</formats>
					<!-- Disable .NET Analyzer -->
					<assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
					<nugetconfAnalyzerEnabled>false</nugetconfAnalyzerEnabled>
					<nuspecAnalyzerEnabled>false</nuspecAnalyzerEnabled>
					<!-- Fail builds where this is used to 7 High to Critical -->
					<failBuildOnCVSS>7</failBuildOnCVSS>
					<!-- Don't fail on error (for cases where CVE REPO is bad) -->
					<failOnError>false</failOnError>
					<skip>${skipDependencyCheck}</skip>
					<suppressionFile>${project.basedir}/dependency-suppression.xml</suppressionFile>
					<outputDirectory>target/site</outputDirectory>
				</configuration>
				<reportSets>
					<reportSet>
						<reports>
							<report>check</report>
						</reports>
					</reportSet>
				</reportSets>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.13</version>
				<reportSets>
					<reportSet>
						<reports>
							<report>report</report>
						</reports>
					</reportSet>
				</reportSets>
			</plugin>
		</plugins>
	</reporting>
</project>
